services:
  - type: web
    name: hubedu-nextjs
    env: node
    plan: starter
    buildCommand: npm run build:render
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /api/health
    autoDeploy: true
    branch: main